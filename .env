# Base URL of the login/signup backend (see src/lib/http/httpClient.ts).
# Must keep the VITE_ prefix — Vite only exposes prefixed variables to renderer code
# (import.meta.env), and the login screen's HTTP calls run in the renderer.
VITE_SERVER_IP=http://localhost:8080
